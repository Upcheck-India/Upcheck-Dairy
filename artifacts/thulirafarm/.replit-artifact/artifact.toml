kind = "mobile"
previewPath = "/"
title = "ThulirFarm"
version = "1.0.0"
id = "artifacts/thulirafarm"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 20028

[services.development]
run = "pnpm --filter @workspace/thulirafarm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/thulirafarm", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/thulirafarm", "run", "serve" ]

[services.env]
PORT = "20028"
BASE_PATH = "/"
